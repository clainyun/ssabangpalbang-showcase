"""AI service test package.

Keeping the test suite as an explicit package ensures imports such as
``tests.support`` resolve to this repository even when a developer machine has
another top-level ``tests`` package installed.
"""
